<div class="form-group">
  <%= form_for(@article) do |a| %>
    <%= a.label :title %>
    <%= a.text_area :title %>
    <%= a.label :content %>
    <%= a.text_area :content %>
    <%= a.submit class:"btn btn-info" %>
  <% end %>
</div>
